import  AppCarousel from '@components/Carousel'
import Products from '../Products'

const ShoppingMall = ()=>{
    return <div>
        <AppCarousel/>
        <Products/>
    </div>
}
export default ShoppingMall